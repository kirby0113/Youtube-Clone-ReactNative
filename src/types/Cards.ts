export type CardProps = {
  videoId: string;
  title: string;
  channelTitle: string;
};

export type LittleCardProps = {
  name: string;
};

export type MiniCardProps = {
  videoId: string;
  title: string;
  channelTitle: string;
};
